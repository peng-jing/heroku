<!doctype html>
<html lang="en">
  <head>
  	<%- include('../template/head') %>
  </head>
  <%- include('../template/navbar') %>
  <div class="container">
		<h2><%= errorMessage %></h2>
		<form method="POST" action="/update_comment/<%= comment.id %>">
			<div class="mb-3">
		    <label class="form-label">編輯留言</label>
		    <textarea name="content" class="form-control"><%= comment.content%></textarea>
		  </div>
		  <button type="submit" class="btn btn-primary">Submit</button>
		</form>
	</div>
</html>